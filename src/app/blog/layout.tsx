import type { Metadata } from "next";
export const metadata: Metadata = { title: "Blog", description: "Expert articles on nutrition, wellness, fitness, AI health technology, and healthy recipes from SVH Nutrition Club." };
export default function BlogLayout({ children }: { children: React.ReactNode }) { return children; }
