import type { Metadata } from "next";
export const metadata: Metadata = { title: "Services", description: "Discover our AI-powered wellness services — body dashboard, voice assistant, meal generator, nutrition scanner, 3D body model, and more." };
export default function ServicesLayout({ children }: { children: React.ReactNode }) { return children; }
