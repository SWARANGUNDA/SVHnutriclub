import type { Metadata } from "next";
export const metadata: Metadata = { title: "Contact Us", description: "Get in touch with SVH Nutrition Club. Reach out for product inquiries, consultations, membership questions, or technical support." };
export default function ContactLayout({ children }: { children: React.ReactNode }) { return children; }
