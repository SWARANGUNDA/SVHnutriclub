import type { Metadata } from "next";
export const metadata: Metadata = { title: "Transformations", description: "See real transformation results from SVH Nutrition Club members — before & after stories powered by AI wellness tracking." };
export default function ResultsLayout({ children }: { children: React.ReactNode }) { return children; }
