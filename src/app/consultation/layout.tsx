import type { Metadata } from "next";
export const metadata: Metadata = { title: "Book Consultation", description: "Book a free wellness consultation with SVH Nutrition Club experts. Choose video, voice, or chat consultation at your convenience." };
export default function ConsultationLayout({ children }: { children: React.ReactNode }) { return children; }
